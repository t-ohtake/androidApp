<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical">
    
      <ListView
        android:id="@+id/TimelineListView" 
		android:layout_width="wrap_content" 
		android:layout_height="wrap_content" 
		android:layout_weight="1"/>

      <!-- 更新ボタン -->
      <Button
        android:id="@+id/updateBtn"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:background="@drawable/image_stateful"
        android:text="@string/updateTimeLineBtn" />
      
      <!-- ツイート画面へ遷移ボタン -->
      <Button
        android:id="@+id/moveTweetBtn"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:background="@drawable/image_stateful"
        android:text="@string/moveTweetBtn" />
      
      <!-- シンプルゲーム画面へ遷移ボタン -->
      <Button
        android:id="@+id/moveSimplegameBtn"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:background="@drawable/image_stateful"
        android:text="@string/moveSimplegameBtn" />

</LinearLayout>